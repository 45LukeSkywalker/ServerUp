# ServerUp
- Simple tool to check whether it is possible to connect to IP addresses
- Add IPs in quotation marks in the servers section of the config.json file, separated by commas
- Then run main.py to see outputs
